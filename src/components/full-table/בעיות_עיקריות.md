# בעיות עיקריות ברכיב full-table

## 1. שמות משתנים לא מתאימים
**דוגמה:** `infoStudents` בפונקציית `useColumnsTable` - משתנה שמתאר נתונים כלליים ולא דווקא סטודנטים

## 2. שימוש בסוגי נתונים גנריים במקום מוגדרים
**דוגמה:** `any[]` בממשק `TableConfig` עבור `tableData` ו-`tableColumns` - מונע type safety

## 3. פונקציות עם חתימות לא עקביות
**דוגמה:** `handleFilters` בקובץ `toolbar.jsx` מחזיר `console.log` במקום הטמעה אמיתית

## 4. עירוב של לוגיקה עסקית בתוך קומפוננטות UI
**דוגמה:** `description` function מיובא מתוך `src/sections/insert/functions.ts` בקובץ `row.jsx`

## 5. שמות פונקציות לא מתאימים למטרתן
**דוגמה:** `useColumnsTable` בקובץ `table.jsx` - שם שמתאר columns אבל הפונקציה מטפלת בכל הטבלה

## 6. קוד דיבוג שנשאר בפרודקשן
**דוגמה:** `console.log(EditComponent)` בקובץ `row.jsx` ב-line 64

## 7. אתחול ערכים לא הגיוני
**דוגמה:** `defaultRowsPerPage: 6` בהוק `useColumnsTable` - מספר לא סטנדרטי לעמודים בדף

## 8. הגדרות hardcoded במקום הועברה כפרמטרים
**דוגמה:** `LINKS` בקובץ `columns-list-view.tsx` מוגדר כקבוע מקומי במקום להיות מועבר כפרמטר

## 9. שימוש בלוגיקה מותנית מורכבת בתוך JSX
**דוגמה:** `headLabels` בקובץ `view.tsx` - מערך שנבנה עם תנאים מורכבים ישירות בתוך הקומפוננטה

## 10. שימוש בפונקציות חץ להגדרת קומפוננטות ללא צורך
**דוגמה:** `ApplyFilter` בקובץ `toolbar.jsx` - קומפוננטה פשוטה שמוגדרת כפונקציית חץ במקום function declaration רגילה