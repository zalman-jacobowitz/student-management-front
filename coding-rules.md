# כללי כתיבת קוד

## 1. ארגון ייבואים

1. ארגן ייבואים בסדר: ספריות חיצוניות, קומפוננטות פנימיות, אוטיליטיז
2. קבץ ייבואים מאותה ספרייה במשפט אחד
3. השתמש בשמות מובנים לייבואים מקובצים
4. הפרד ייבואים עם שורות ריקות לקריאות טובה יותר
5. השתמש במסלולים יחסיים עקביים עם קידומת `src/`

## 2. פונקציות ותצורות

1. השתמש בפונקציות חץ קצרות ונקיות עם return מפורש
2. בנה שרשור פעולות באופן קריא עם שבירת שורות
3. פרק פונקציות מורכבות לפונקציות עזר קטנות יותר
4. השתמש ב-useCallback לפונקציות שמועברות כ-props או dependencies
5. הגדר ערכי ברירת מחדל לפרמטרים של פונקציות

## 3. ניהול State ו-Forms

1. השתמש ב-useState למצב מקומי פשוט עם ערכי התחלה ברורים
2. השתמש ב-useFormContext כדי לגשת לפורם נוכחי בקומפוננטות צאצא
3. השתמש ב-useFieldArray לניהול רשימות דינמיות בפורמים
4. השתמש ב-Controller עבור קומפוננטות פורם מותאמות אישית
5. הצג שגיאות פורם בצורה עקבית

## 4. מבנה קומפוננטות ו-UI

1. ארגן קומפוננטות עם מבנה עקבי: ייבואים, הגדרות, קומפוננטה, ייצוא
2. השתמש ב-destructuring בפרמטרי פונקציה עם ערכי ברירת מחדל
3. השתמש בהפצת props (...other) להעברת props נוספים
4. השתמש ב-conditional rendering עם אופרטורים לוגיים
5. ארגן props באופן לוגי: ראשיים, אירועים, styling